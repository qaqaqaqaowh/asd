<h1>Listings Index</h1>

<% @listings.each do |l| %>
	<%= link_to l.name, listing_path(l) %><br>
<% end %>

<%= link_to "New Listing", new_listing_path %>
<%= link_to "Show My Listing", my_listings_path %>